specVersion: 0.0.5
description: Tashi blockchain
repository: https://github.com/ChaosLabsInc/subgraph-tashi
schema:
  file: ./schema.graphql
dataSources:
  - kind: cosmos
    name: Comptroller
    network: cosmoshub-4
    source:
      startBlock: 0 # first MarketListed event on https://snowtrace.io/block/3046885
    mapping:
      apiVersion: 0.0.6
      language: wasm/assemblyscript
      file: ./src/mappings/comptroller.ts
      entities:
        - Comptroller
        - Market
      eventHandlers:
        - event: MarketListed(address)
          handler: handleMarketListed
        - event: MarketEntered(address,address)
          handler: handleMarketEntered
        - event: MarketExited(address,address)
          handler: handleMarketExited
        - event: NewCloseFactor(uint256,uint256)
          handler: handleNewCloseFactor
        - event: NewCollateralFactor(address,uint256,uint256)
          handler: handleNewCollateralFactor
        - event: NewLiquidationIncentive(uint256,uint256)
          handler: handleNewLiquidationIncentive
        # - event: NewMaxAssets(uint256,uint256)
        #   handler: handleNewMaxAssets
        - event: NewPriceOracle(address,address)
          handler: handleNewPriceOracle
